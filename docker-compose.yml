version: '3.9'
services:
  ssh.cyrhades.fr:
    build: ./.docker/ssh
  webssh.cyrhades.fr:
    build: ./.docker/webssh
    ports:
      - ${PORT}:8888
  au-boolot.cyrhades.fr:
    image: anssi/fcsc2023-hardware-au-boolot:latest
    ports:
      - ${PORT_SSH}:4000
